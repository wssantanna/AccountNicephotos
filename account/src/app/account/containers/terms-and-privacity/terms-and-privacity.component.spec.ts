import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TermsAndPrivacityComponent } from './terms-and-privacity.component';

/*
describe('TermsAndPrivacityComponent', () => {
    let component: TermsAndPrivacityComponent;
    let fixture: ComponentFixture<TermsAndPrivacityComponent>;

    beforeEach(async () => {
        await TestBed.configureTestingModule({
            declarations: [TermsAndPrivacityComponent]
        })
            .compileComponents();
    });

    beforeEach(() => {
        fixture = TestBed.createComponent(TermsAndPrivacityComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });

    it('should create', () => {
        expect(component).toBeTruthy();
    });
});
*/
